服务器登录：ssh ubuntu@124.220.79.133
9?d~+4%p8aQj]CYA











git add .
git commit -m ""
git push origin main
更新服务器的代码：
cd ~/desk && \
git reset --hard && \
git pull && \
ls -la


程序运行：
sudo ./start.sh


cd ~/desk/Moon_Dance && \
sudo bash start.sh
查看容器整体
sudo docker ps
单独查看容器：
sudo docker logs -f simulator-5
强制修改容器个数：
cd ~/desk/Moon_Dance && \
sed -i 's/NUM_CONTAINERS=10/NUM_CONTAINERS=30/g' start.sh && \
cat start.sh | grep NUM_CONTAINERS
清理垃圾数据
cd ~/desk/Moon_Dance && \
echo "data/" > .dockerignore && \
echo "__pycache__/" >> .dockerignore && \



sudo bash start.sh
查看窗口
watch -n 1 free -m